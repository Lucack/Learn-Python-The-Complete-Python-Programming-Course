import sys

    # sys.stdin  for the input
input = sys.stdin.readline(10)

#This sentence is going to be read by the function we just entered

print(input) # only the 10 characters

    # sys.stdout 
sys.stdout.write("this statement is awesome!")

    # sys.version
print(sys.version)

